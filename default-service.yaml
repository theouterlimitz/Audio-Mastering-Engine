# default-service.yaml
# Configuration for the STAGING frontend service.

runtime: python312
service: staging-default  # <-- This is the key change
instance_class: F2

entrypoint: gunicorn -b :$PORT main:app

