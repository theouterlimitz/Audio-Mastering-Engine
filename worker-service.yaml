# worker-service.yaml
# The final, correct configuration for the STAGING backend worker service.

runtime: python312
service: staging-worker  # <-- This is the critical fix. The name now matches.
instance_class: B8

manual_scaling:
  instances: 1

entrypoint: gunicorn --timeout 600 -b :$PORT worker:app

