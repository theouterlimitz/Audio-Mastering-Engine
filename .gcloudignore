# .gcloudignore
# This file tells the gcloud command-line tool which files to ignore when deploying.

# The .gcloudignore file itself should be ignored.
.gcloudignore

# Git revision control directory.
.git/
.gitignore

# The Dockerfile is used to build the container image on Google Cloud.
Dockerfile

# Python virtual environment directories.
venv/
env/

# Python bytecode and cache.
*.pyc
__pycache__/

# IDE and editor configuration files.
.idea/
.vscode/

# --- THIS IS THE CRITICAL FIX ---
# Do not upload the service account key by default for security...
# *.json

# ...BUT create a specific exception to ALWAYS include our key file.
!sa-key.json

